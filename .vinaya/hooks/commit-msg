#!/usr/bin/env sh
# >>> vinaya:managed:commit-msg >>>
# Vinaya commit-message gate. Validates the message's first line against
# this repo's Type(scope): Description convention.
npx --yes @attalabs/vinaya@0.19.3 commit-msg "$1" "$2" || exit 1
# <<< vinaya:managed:commit-msg <<<
